# Jaga frontend environment (example)
#
# No real endpoint URLs are committed. The triage/cxr contracts are gated on
# Daffa's ARCH-1 (project-architecture.md §6); leave API base EMPTY until signed.

# Base URL for the Go API gateway. Intentionally blank for now.
NEXT_PUBLIC_API_BASE_URL=

# App environment label only (no behavior depends on it yet).
NEXT_PUBLIC_APP_ENV=development
