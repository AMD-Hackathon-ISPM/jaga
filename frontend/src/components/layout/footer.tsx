/** Footer — persistent research disclaimer (locked safety language, design §2). */
export function Footer() {
  return (
    <footer className="mx-auto w-full max-w-flow px-4 py-8 text-sm text-ink-muted">
      <p>
        Jaga is an investigational research prototype. It does not diagnose or rule out tuberculosis.
        Every participant should receive confirmatory evaluation.
      </p>
    </footer>
  );
}
