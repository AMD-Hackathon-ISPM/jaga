/**
 * Sidebar — OPERATOR-AREA SCAFFOLDING ONLY.
 *
 * Not part of the single-column capture flow (design §5.6: the flow never
 * becomes a multi-column dashboard). Placeholder for a future operator area.
 */
export function Sidebar() {
  return (
    <aside className="w-56 shrink-0 border-r border-border-subtle bg-surface p-4">
      <p className="text-sm text-ink-muted">Operator navigation placeholder.</p>
    </aside>
  );
}
