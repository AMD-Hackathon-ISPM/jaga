# Go Service Scaffold

This module is a thin orchestration layer around the Python ML package in `../python/project`.

## Run

```bash
go run ./cmd/server
```

## Environment

- `JAGA_BACKEND_ADDR`
- `JAGA_PYTHON_PROJECT_ROOT`
