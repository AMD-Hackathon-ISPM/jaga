# Jaga · Change & Decision Log

**Document type:** Historical change ledger
**Audience:** All contributors
**Status:** Active
**Canonical for:** Chronology of significant documentation and product decisions
**Companion documents:** [`context-dump.md`](context-dump.md), [`../AGENT.md`](../AGENT.md)

## How to read this document

Entries are newest first. Older entries preserve what was decided at that time and may be superseded by later entries. Current decisions belong in [`context-dump.md`](context-dump.md) Section 16; rejected decisions belong in Section 17.

---

- 2026-07-01 — **Prepared the frontend for contract-first backend integration without changing backend code.** Added fixture/live adapters and runtime schemas for capability status, signed patient intake, Gema multipart submission with exactly five WebM cough files, independent Prisma digital-CXR upload, and a scoped non-streaming guidance assistant behind the Go gateway. Added separate in-memory Gema/Prisma results, routed CXR upload/result surfaces, server-field error mapping, synthetic fixtures, tests, environment flags, and the machine-readable frontend proposal at `contracts/openapi/jaga-v1.yaml`. Fireworks/Featherless credentials remain server-side; assistant requests contain only locale, screen context, optional field key, and bounded chat turns—not patient form values. Gema and Prisma remain separate and never fused. Backend implementation and Daffa's `ARCH-1` sign-off remain open. (Codex)

- 2026-07-01 — **Migrated the five routed frontend surfaces and shared layout to shadcn/ui preset `b85jYWWKi8` on Tailwind 4.** Adopted official Button, Card, Field, Input, RadioGroup, ToggleGroup, Alert, Empty, Badge, Skeleton, Spinner, Item, and Accordion APIs while preserving Jaga's signed light-only OKLCH tokens, 44 px targets, focus/reduced-motion rules, result safety hierarchy, and the flow layout, canvas cough waveform, risk track, and step indicator. Figtree and EB Garamond now use `next/font/google`; Ioskeley Regular is self-hosted through `next/font/local`. RHF RadioGroup controllers now emit booleans directly without changing `ClinicalFormValues` or the snake_case contract. Backend, API, store, route, and wire types are unchanged. (Codex)

- 2026-06-30 — **Initialized shadcn/ui for the frontend using the Radix/Nova configuration.** Added `frontend/components.json`, the current shadcn dependencies and CSS utilities, and semantic shadcn variables mapped to Jaga's signed light-theme tokens. Preserved the existing token-styled UI primitives and behavior; future components should be sourced through the shadcn CLI and adapted to the signed token contract. This supersedes the earlier decision that shadcn was intentionally absent without rewriting that historical entry. Updated `project-architecture.md` §3.2, `context-dump.md` §16, and the frontend handoff documentation. (Codex)

- 2026-06-30 — **Renamed the frontend folder `apps/web/` → `frontend/` and adopted Radix under the bespoke token skin.** Overrides the §11/§3.2 reserved `apps/web` name (the frontend was never an npm workspace, so the rename is config-free); updated the relative doc links inside the moved `README.md`/`DEVELOPMENT.md` (`../../` → `../`) and the path references in `project-architecture.md` §3.2/§11. Documented the two root-level non-application folders in §11: `components/` (the provisional `ClinicalCaptureForm.jsx` reference prototype) and `design/` (`swatch.html` + `contrast.mjs` WCAG tooling). Removed the empty root `package-lock.json` stub. Layered **Radix** (`@radix-ui/react-dialog`) under the existing OKLCH-token skin for `ui/dialog.tsx` (proper focus trap/scroll-lock/portal); `Modal` stays an alias and every token class is preserved, so the bespoke "no default shadcn theme" rule still holds. Token coloring and Tailwind were already in place and unchanged. (Billy, via Cowork)

- 2026-06-29 — **Built out the frontend capture pieces and documented them for handoff.** Implemented the full clinical form (all 13 signed Go fields via RHF + Zod, bounds mirrored from `validation/patient.go`) and fixed a radio-validation bug (RHF ignores `setValueAs` on radio/checkbox, so yes/no answers submitted the string `"true"` and failed `z.boolean()`; now coerced in the Zod schema). Added a functional cough recorder: Web Audio `AnalyserNode` + a full-bleed `<canvas>` voice-memo waveform (scrolling right→left, reduced-motion level-meter alternative) and a round, glowing, breathing record orb (mic/stop icon, gradient + pulse, motion disabled under `prefers-reduced-motion`); mic permission requested only on tap, no audio persisted or uploaded. Added a keyed/versioned EN/ID locale scaffold + `useT` (safety strings left English, flagged `PENDING UX-1`, never machine-translated). Wrote `apps/web/DEVELOPMENT.md` — a full developer guide (routing, state, types/Go-mirror discipline, forms, the recorder internals, services, i18n, tokens, component catalog, a11y, extension how-tos, gotchas). `tsc --noEmit` clean. Still no API calls / no endpoint URLs; triage/cxr contracts remain gated on `ARCH-1`, paired ID strings on `UX-1`. (Kei)
- 2026-06-29 — **Scaffolded the frontend foundation (FE-0 groundwork).** Added a Next.js (App Router) PWA client implementing the signed design direction and screen map (gate → clinical → coughs → review → result), placeholders and mock data only — no API calls, no endpoint URLs, no backend/infra changes. `types/patient.ts` mirrors `models/patient.go` and the clinical Zod schema mirrors `validation/patient.go` bounds; `types/triage.ts` and `types/cxr.ts` are explicitly provisional pending Daffa `ARCH-1`. Session state is an in-memory Zustand store with no `persist` (honors the no-localStorage/transient-input rule); services throw `API not connected yet`; Axios is configured but unused. UI primitives are hand-rolled to the §4 OKLCH tokens (shadcn intentionally not added). Auth (AuthContext + Protected/Guest guards + mock store) and Navbar/Sidebar are placeholder operator-area scaffolding only — the public triage flow stays account-less. Added a keyed/versioned EN/ID locale scaffold; mandatory safety/referral strings are left in English pending `UX-1` and flagged, never machine-translated. Tech: React 19 · Next 15 · TS · Tailwind · TanStack Query · Zustand · RHF · Zod · ESLint · Prettier. Lives under `apps/web/` (the architecture §11/§3.2 reserved name); dependencies installed and the Next dev server compiles. (Kei)
- 2026-06-29 — **Removed Ollama from the semantic-memory path.** Cognee stays local for memory while Featherless (OpenAI-compatible) handles generation. (Codex)
- 2026-06-29 — **Synced `.agent` to the current `backend-train` runtime layout.** `PrismaTraining` for research, `PrismaServer` for serving, stack-first Docker Swarm deployment, bundled `local_clahe` server artifacts, and optional Cognee semantic memory wired into the Go backend. This supersedes the `backend/python/project` naming used in the 2026-06-28 Prisma-promotion entry below. (Codex)
- 2026-06-28 — **Added `/infra` as a Docker Swarm deployment plane.** NGINX ingress, replicated `go-api`, internal `prisma-worker`, Redis, PostgreSQL, and MinIO; recorded Featherless as an OpenAI-compatible integration target. (Codex)
- 2026-06-28 — **Added a Go-only patient intake module at `POST /api/v1/patient/intake`** to validate and normalize metadata before any future Prisma cough or CXR inference; no persistence or ML calls yet. (Codex)
- 2026-06-28 — **Added the embedding-first TB-CXR research scaffold** under `backend/python/PrismaTraining` (interchangeable backbones, training/eval pipelines, embedding export, retrieval over saved embeddings, post-training quantum branch) plus a thin `backend/go` service scaffold; documented as a stretch CXR track that does not alter the cough+clinical MVP invariant. (Codex)
- 2026-06-28 — **Promoted Prisma (digital-CXR) to a co-equal `[MVP]` signal and aligned the docs with the `backend-train` branch.** The `backend-train` branch has built the CXR side first: `backend/python/project` is a "Retrieval-Augmented TB Chest X-ray Classification" PyTorch framework (interchangeable backbones DenseNet121 / EfficientNet-B0 / BiomedCLIP / Rad-DINO emitting unified `embedding`+`logits`, FAISS retrieval with evidence aggregation, Grad-CAM, YAML configs incl. a CLAHE variant), plus a thin Go orchestration scaffold (`backend/go`, only `/healthz` + `/v1/status`). That work corresponds to **Prisma**, which the docs had marked isolated `[Stretch]`/P2 — an inverted priority. Per team decision, **Gema (cough+clinical) and Prisma (digital-CXR) are now co-equal `[MVP]` signals, built in parallel, evaluated separately, and never fused.** Threaded the relabel and de-gating through `README.md`, `AGENT.md` (§3, §8), `product-brief.md` (phase table), `product-requirements.md` (PRD-09 + rules + traceability), `project-architecture.md` (§4.1 module labels, §6 added a separate `POST /api/v1/cxr` operation, §12.1 failure isolation), `data-evaluation-plan.md` (§2.2 + new §5.3 Prisma CXR models/evidence gate + leakage rule), and `implementation-plan.md` (ML-5/FE-7 → P0, fallback, risk). The "never fused", source-leakage, and photographed-film-out rules are preserved. **Still divergent / open:** the **Gema cough+clinical backend has no implementation yet** (no audio, CODA, quality gate, or calibration code on `backend-train`), and **no FastAPI inference service exists** — the Go service is health/status stubs, so `POST /api/v1/triage` and `POST /api/v1/cxr` remain unbuilt; the implemented layout (`backend/python` + `backend/go`) differs from the planned `apps/api/` FastAPI service in architecture §11. Daffa's `ARCH-1` and pipeline owner-blocks (due 2026-06-29) still gate the signed contracts, including the new Prisma model/§5.3 and `/api/v1/cxr` blocks. (Billy)
- 2026-06-28 — **Palette + type refinement after a design-taste pass.** Reworked the swatch and synced `design-guidelines.md`: canvas `#FFFFEB` (pale cream), brand `#024F46` (deep pine green, actions only), ink `#1A1A1A`; serif **EB Garamond**, sans **Figtree**, numerics still Ioskeley Mono. Rebuilt the risk ramp (light taupe → ochre → deep brick, still non-green/monotonic/CVD-safe) and the spectrogram map; re-verified all WCAG ratios via `design/contrast.mjs` (ink/canvas 17.2:1, brand 9.39:1). Tightened to one radius scale (8 px controls/cards, 4 px bars, 16 px device frame, pill for the toggle) and a strict 4px spacing rhythm. `.impeccable/` critique archive is excluded locally via `.git/info/exclude` (no `.gitignore`). (Billy)
- 2026-06-28 — **Signed the frontend design guidelines (warm "mymind/Wisprflow" direction) and flipped the default language.** Replaced the deferred Billy owner blocks in `design-guidelines.md` with implementable specs: a committed **warm cream** palette (OKLCH tokens with computed WCAG 2.2 contrast evidence in §4.6, light-only for MVP); a 3-role type system — **Fraunces** display serif + **IBM Plex Sans** body + **Ioskeley Mono** (self-hosted, OFL) for all numerics; the ordered **screen/state map** demographic→coughs→result with a state machine; the **iOS-Voice-Memos** right→left `<canvas>` cough visualizer (reduced-motion + text alternatives); and the result-reveal + spectrogram spec (locked hierarchy, safety content never gated behind motion, non-green risk ramp so "Lower" never reads as clearance). Framed the whole system as **"constraints are a budget, not a ceiling"** — low-end Android sets a performance budget, not an aesthetic limit — superseding the old "dependable not futuristic / avoid motion" guardrail. Synced `project-architecture.md` §3.2 (signed FE-0: Next.js + Tailwind + Web Audio/canvas, folder structure) and `implementation-plan.md` UX-0/UX-1 status. **Changed PRD-07:** English is now the default field language with a one-tap Bahasa Indonesia toggle (both complete/reviewed/paired), for the English-judged hackathon demo; layouts are sized for the longer language. Still open: the paired EN/ID string table (`UX-1`) and exact API error codes (Daffa `ARCH-1`, due 2026-06-29). (Billy)
- 2026-06-28 — **Locked module names: Gema and Prisma.** Named the two research signals to reinforce that they are separate and never fused: **Gema** ("echo") = the `[MVP]` cough-plus-clinical core; **Prisma** ("prism") = the isolated `[Stretch]` digital-CXR module. Threaded through `product-brief.md`, `product-requirements.md` (PRD-09), `project-architecture.md` (module boundaries + stretch-failure table), `data-evaluation-plan.md` (§2.2), and `README.md`; added a Decisions-Index row and glossary entries in `context-dump.md`. No slogan locked yet. (Billy)
- 2026-06-28 — **Registered the Selisios 2026 fused baseline (CODA-10).** Added the published reproducible baseline (Selisios, *Sensors* 26(4):1223, DOI 10.3390/s26041223) to `evidence-register.md` §3 as the fused audio-plus-clinical reference Jaga mirrors (CODA solicited set, LR + CatBoost, cougher-disjoint nested CV, isotonic calibration, conformal prediction). Verified the paper encodes clinical inputs as continuous + 0/1 binaries with no missing values; it does **not** state a one-hot "prior TB type" expansion or a "16-feature" count, so that wording is flagged for Daffa to confirm against the CODA data dictionary before use. Drafted the provisional `clinical-v1` capture form (PRD-01 gate + PRD-02 fields) marked schema-pending-signature pending Daffa's inference contract (due 2026-06-29). (Billy)

- 2026-06-28 — **Trellis-depth planning revision.** Made `AGENT.md` the sole entry point and removed the duplicate secondary agent entry; standardized document metadata, scope labels, source precedence, owner blocks, and contradiction comments; corrected CODA access/cohort/five-cough facts, WHO 2025 tiered profiles, event dates, criteria, containerization, and public-repository requirements; locked the research-only safety boundary and confirmatory evaluation for every symptomatic adult; separated Daffa/Zeddin backend-AI duties and Billy/Kei frontend duties; expanded the PRD, architecture, evaluation, design, and implementation board with contracts, states, acceptance criteria, fallbacks, and traceability. Preparation starts 29 June; the official sprint is 6–11 July. This entry supersedes earlier current-state claims about a validated core, public CODA, a universal 90/70 gate, offline/fused MVP behavior, and the retained secondary pointer. (Billy)
- 2026-06-27 — **Documentation & evidence overhaul.** Added `evidence-register.md` (single source of truth for facts), `data-evaluation-plan.md` (datasets/splits/leakage/model-gate/metrics), `implementation-plan.md` (5-day board + API contract). Upgraded `AGENT.md` into a task router with source precedence and kept a secondary agent entry point. Verified facts: **CODA is controlled-access (Daffa has it via ORCID)**; cohort = symptomatic adults 18+, guided coughs; **WHO 2025 screening TPPs** replace the old 90/70; **event window 6–11 Jul 2026**. Reframed as an **investigational research prototype** while keeping the product/market ambition. Set aside **quantum image processing** (no advantage, weakens AMD story). (Billy)
- 2026-06-27 — **Phase 13 honest revision** (after a hard technical review, facts verified): switched to **online cloud serving on AMD** (dropped offline/on-device claims, kept on-prem edge as roadmap); **cough+clinical = validated core, CXR optional/stretch** (no paired data → no fused metric); **honest metrics** (cough+clinical AUROC ~0.78–0.83 cited as _published CODA, not a Jaga result_; WHO 90/70 reframed as aspiration); corrected stats (WHO 2025 ~2.4M detection gap, CAD may replace radiologists, Swaasa parity); removed "confidence always rises"; fixed privacy wording; cut plan to ~5-day sprint; added MIT LICENSE; reconciled "Stack TBD"; fixed dangling plan-jaga.md ref. (Billy)
- 2026-06-27 — Reframed positioning **global-first** (TB high-burden belt worldwide) with Indonesia as the named beachhead/authenticity anchor, not the ceiling. Updated product-brief, README, pitch messages, and context-dump. (Billy)
- 2026-06-27 — Added governance layer: scope tags + Do-Not-Build in the PRD, Rules for Agents in `AGENT.md`, per-doc status headers, this log, and a maintenance ritual in the secondary agent entry file. (Billy)
- 2026-06-27 — Added README.md (Trellis-style) and AGENT.md entry point; removed stale `plan-medical.md`. (Billy)
- 2026-06-27 — Split the project plan into four spec docs: `product-brief.md`, `product-requirements.md`, `project-architecture.md`, `design-guidelines.md`. (Billy)
- 2026-06-27 — Added Fransisco to the team. (Billy)
- 2026-06-27 — Validated business model (TB diagnostics market + Global Fund + Qure.ai comparable). (Billy)
- 2026-06-27 — **Locked name: Jaga** ("to watch over / guard"). (Billy)
- 2026-06-27 — **Locked idea: offline multimodal TB triage** (cough required + optional symptoms/X-ray → on-device verdict). Triage, not diagnosis. See `context-dump.md` §12, §14. (Billy)
